{
  "$schema": "https://vomaste.cz/schemas/canonical/tooling-command.schema.json",
  "schemaVersion": 1,
  "@context": "https://vomaste.cz/context/v1.jsonld",
  "@id": "https://vomaste.cz/id/prikazy/build",
  "@type": "vomaste:ToolingCommand",
  "recordType": "toolingCommand",
  "identifier": "build",
  "kind": "npm",
  "name": "build",
  "title": "Plný build — brána kvality",
  "category": "provoz",
  "summary": "Jediný orchestrační vstup build pipeline. Deleguje na scripts/build/pipeline.mjs, který drží pořadí kroků jako data i s důvody, proč je právě takové.",
  "enforces": [
    "Nenulový exit kteréhokoli kroku pipeline okamžitě zastaví běh — pipeline nepokračuje a vypíše, který krok selhal a s jakým kódem."
  ],
  "whenToRun": "Před každým mergem nebo pushem. Tohle je TA brána kvality: pre-commit hook je rychlá podmnožina, ne náhrada. Stejnou sekvenci volá i CI, což hlídá check:workflow-parity.",
  "inputs": [
    "celý repozitář"
  ],
  "outputs": [
    "public/",
    "data/generated/**",
    "static/data/**",
    "static/css/main.css",
    "static/js/*.js"
  ],
  "sourceFile": "scripts/build/pipeline.mjs",
  "describedFrom": "header",
  "notes": [
    "Každý režim začíná `data:validate` — nevalidní kanonická data musí pipeline zastavit dřív, než cokoli vygeneruje.",
    "Režim build drží zámek po celý běh, ne jen na kroku zola build: dva souběžné buildy ve stejném checkoutu se dřív přetahovaly už o data/generated/views/**."
  ]
}
