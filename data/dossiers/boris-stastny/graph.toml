# Relationship graph for the Boris Šťastný dossier. Projection of the
# claim/source registries — every edge carries at least one CLM-## and one
# SRC-## that exist there. Minimal by design: an edge exists only where a
# claim and an opened source support it.

[[nodes]]
id = "stastny"
type = "person"
label = "Boris Šťastný"
depth = 0
subject = true
claims = ["CLM-01"]
sources = ["SRC-01"]

[[nodes]]
id = "vlada"
type = "public_institution"
label = "Vláda ČR"
depth = 1
subject = false
claims = ["CLM-01"]
sources = ["SRC-01"]

[[edges]]
id = "edge-stastny-vlada"
source = "stastny"
target = "vlada"
relation = "HOLDS_ROLE"
label = "ministr pro sport, prevenci a zdraví"
status = "contextual"
claims = ["CLM-01"]
sources = ["SRC-01"]
