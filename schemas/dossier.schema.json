{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vomaste.cz/schemas/dossier.schema.json",
  "title": "Dossier registry row (record-tables projection of data/dossiers.toml)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "title",
    "dossier_type",
    "url"
  ],
  "properties": {
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "dossier_type": {
      "type": "string",
      "enum": [
        "entity",
        "aggregate"
      ],
      "$comment": "Structural invariants between the two types (who owns physical records, who is a generated view) are owned by validate:dossier-types."
    },
    "subject": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "canonical_dossier": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "url": {
      "type": "string",
      "pattern": "^/dossiers/[a-z0-9-]+/$"
    },
    "description": {
      "type": "string",
      "description": "Popis z front matter dossieru; prezentační, nikdy autorský soud."
    },
    "updated": {
      "type": "string",
      "description": "Datum poslední změny obsahu (ISO 8601), z front matter."
    },
    "reviewed_at": {
      "type": "string",
      "description": "Datum poslední redakční kontroly proti zdrojům (ISO 8601). NENÍ to datum změny souboru."
    },
    "counts": {
      "type": "object",
      "description": "Počty záznamů z data/dossiers/<slug>/stats.toml. Generované — nikdy psané ručně, jinak se rozejdou s registry.",
      "properties": {
        "claims": {
          "type": "integer",
          "minimum": 0
        },
        "sources": {
          "type": "integer",
          "minimum": 0
        },
        "cases": {
          "type": "integer",
          "minimum": 0
        },
        "gaps": {
          "type": "integer",
          "minimum": 0
        },
        "entities": {
          "type": "integer",
          "minimum": 0
        },
        "relations": {
          "type": "integer",
          "minimum": 0
        }
      },
      "required": [
        "claims",
        "sources",
        "cases",
        "gaps",
        "entities",
        "relations"
      ],
      "additionalProperties": false
    },
    "routes": {
      "type": "object",
      "description": "Routy dossieru a jeho registrů, ČTENÉ z data/generated/navigation.json. Neskládají se z řetězců: manifest je kanonický, takže přejmenování registru se projeví na jednom místě.",
      "additionalProperties": {
        "type": "string",
        "pattern": "^/"
      }
    },
    "route_labels": {
      "type": "object",
      "description": "Lidské popisky registrů z téhož manifestu, aby je adresář nemusel psát znovu.",
      "additionalProperties": {
        "type": "string"
      }
    }
  }
}
